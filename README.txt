Structure of the project:
          lb : home folder of loadbalancer instance
          webserver -> source : code not instrumented
                    -> instrumented : instrumented code
                    -> config.sh : classpath configuration

aws_version = aws-java-sdk-1.11.1024
